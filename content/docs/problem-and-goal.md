# Problem and Goal

## Problem

GPU compute for AI and other workloads is expensive and concentrated in centralized cloud platforms, while a large amount of GPU capacity remains underused in gaming machines, mining rigs, small data centers, and enterprise clusters.

A direct rental market between strangers has several trust problems:

- A buyer cannot easily know whether the job really ran.
- A provider may not know whether the buyer will pay.
- A provider can disappear while a job is running.
- A buyer can claim a valid result is invalid.
- Hardware claims may be inaccurate.
- Pricing is fragmented across independent providers.
- There is no shared reputation history.

## BRUT's Goal

BRUT introduces a common protocol for the economic relationship between buyer and provider.

It provides:

- standardized job requirements,
- provider inventory and pricing,
- escrowed payment,
- job status tracking,
- verification inputs,
- reputation,
- optional provider stake,
- slashing rules,
- and transparent onchain settlement.

The system does not claim that every offchain fact can be perfectly proven. Instead, it separates directly onchain facts from attested or provider-reported information.
